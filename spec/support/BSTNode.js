class BSTNode {
  constructor(data){
    this.data = data;
    this.left = null;
    this.right = null;
  }

  compareToOtherTree(other){
    // TODO: compare all nodes in this to other and return true if data matches
  }
}

export default BSTNode;
